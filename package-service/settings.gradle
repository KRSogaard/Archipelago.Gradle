rootProject.name = "package-service"

if (file("../archipelago-common").exists()) {
    includeBuild "../archipelago-common"
}
// Adding this breaks it for some reason
//if (file("../archipelago-common-github").exists()) {
//    includeBuild "../archipelago-common-github"
//}
if (file("../archipelago-common-rest").exists()) {
    includeBuild "../archipelago-common-rest"
}
if (file("../Archipelago.Acccount.Common").exists()) {
    includeBuild "../Archipelago.Acccount.Common"
}

if (file("../package-service-client").exists()) {
    includeBuild "../package-service-client"
}
if (file("../package-service-models").exists()) {
    includeBuild "../package-service-models"
}
if (file("../package-service-core").exists()) {
    includeBuild "../package-service-core"
}
