rootProject.name = "maui-graph"

if (file("../archipelago-common").exists()) {
    includeBuild "../archipelago-common"
}

if (file("../Maui.Common").exists()) {
    includeBuild "../Maui.Common"
}

if (file("../PackageService.Models").exists()) {
    includeBuild "../PackageService.Models"
}
